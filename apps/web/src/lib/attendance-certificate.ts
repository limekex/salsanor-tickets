/**
 * Shared constants for attendance certificate eligibility.
 * Used by both the API route and the participant portal UI.
 */

/** Minimum attendance rate (0–1) to earn a certificate */
export const CERTIFICATE_MIN_RATE = 0.75

/** Minimum number of scheduled sessions before a certificate can be issued */
export const CERTIFICATE_MIN_SESSIONS = 4
